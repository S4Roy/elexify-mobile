<manifest xmlns:android="http://schemas.android.com/apk/res/android">
  <uses-permission android:name="android.permission.INTERNET"/>
  <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
  <application android:name=".MainApplication" android:label="@string/app_name" android:icon="@mipmap/ic_launcher" android:roundIcon="@mipmap/ic_launcher_round" android:allowBackup="false" android:theme="@style/AppTheme" android:usesCleartextTraffic="${usesCleartextTraffic}" android:supportsRtl="true" android:fullBackupContent="@xml/secure_store_backup_rules" android:dataExtractionRules="@xml/secure_store_data_extraction_rules">
    <!-- Push notification look: monochrome icon; channel and colour come
         from firebase.json (react-native-firebase). -->
    <meta-data android:name="com.google.firebase.messaging.default_notification_icon" android:resource="@drawable/ic_stat_notification"/>
    <activity android:name=".MainActivity" android:label="@string/app_name" android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|screenSize|smallestScreenSize|uiMode" android:launchMode="singleTask" android:windowSoftInputMode="adjustResize" android:exported="true" android:screenOrientation="portrait">
      <intent-filter>
        <action android:name="android.intent.action.MAIN"/>
        <category android:name="android.intent.category.LAUNCHER"/>
      </intent-filter>
      <intent-filter>
        <action android:name="android.intent.action.VIEW"/>
        <category android:name="android.intent.category.DEFAULT"/>
        <category android:name="android.intent.category.BROWSABLE"/>
        <data android:scheme="elexify"/>
        <data android:scheme="exp+elexify-mobile"/>
      </intent-filter>
      <!-- Android App Links: elexify.online links open the matching screen
           (mapped in app/+native-intent.tsx). Verified by
           https://elexify.online/.well-known/assetlinks.json. www is left out
           because it redirects to the apex and so can't pass verification.
           /checkout, /login and password-reset pages stay on the web so
           browser payment callbacks and email flows aren't hijacked. -->
      <intent-filter android:autoVerify="true">
        <action android:name="android.intent.action.VIEW"/>
        <category android:name="android.intent.category.DEFAULT"/>
        <category android:name="android.intent.category.BROWSABLE"/>
        <data android:scheme="https" android:host="elexify.online"/>
        <data android:path="/"/>
        <data android:pathPrefix="/product/"/>
        <data android:pathPrefix="/products"/>
        <data android:pathPrefix="/category/"/>
        <data android:pathPrefix="/product-category/"/>
        <data android:pathPrefix="/categories"/>
        <data android:pathPrefix="/cart"/>
        <data android:pathPrefix="/account"/>
        <data android:pathPrefix="/track-order"/>
        <data android:pathPrefix="/compare"/>
        <data android:pathPrefix="/faq"/>
        <data android:pathPrefix="/contact-us"/>
        <data android:pathPrefix="/about-us"/>
        <data android:pathPrefix="/privacy-policy"/>
        <data android:pathPrefix="/terms-conditions"/>
        <data android:pathPrefix="/refund-cancellations-policy"/>
        <data android:pathPrefix="/page/"/>
      </intent-filter>
    </activity>
  </application>
</manifest>
